\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{tabularx}
\usepackage{array}

\geometry{margin=2.5cm}
\setlength{\emergencystretch}{2em}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue,
  pdftitle={LLM Environment Open Data},
  pdfauthor={Codex}
}

\title{LLM Environment Open Data:\\an Open Dataset, API, and MCP Server for the Quantitative Literature on the Environmental Footprint of Large Language Models}
\author{Working Paper}
\date{March 12, 2026}

\begin{document}

\maketitle

\begin{abstract}
The literature on the environmental footprint of large language models now provides a growing number of quantified indicators, but these results remain scattered across journal articles, model cards, technical reports, and institutional syntheses. This dispersion slows comparison across studies and limits cumulative reuse. We present \emph{LLM Environment Open Data}, a structured dataset that extracts, normalizes, and documents quantitative values reported on LLM training, inference, infrastructure, and lifecycle impacts. The project comprises four complementary artifacts: an open tabular dataset, a layer of comparability metadata, a local HTTP API, and an MCP server for agent-oriented querying. The initial release contains 49 records spanning 11 sources, and links each value to its unit, system boundary, analytical scope, and precise source locator. We further show how this evidence base can support a first environmental externalities estimator at the prompt, software feature, and annualized usage levels. This paper describes the dataset construction process, its structure, its limitations, and its value for both reproducible quantitative reviews of AI environmental impacts and environmentally responsible software design.
\end{abstract}

\textbf{Keywords:} LLMs, open data, dataset, environmental footprint, API, MCP, quantitative review.

\section{Introduction}
Debate on the environmental footprint of LLMs has intensified as publications have multiplied on training, inference, water use, data centers, and hardware lifecycle effects \cite{strubell2019,rillig2023,luccioni2023,ren2024,morrison2025,elsworth2025}. Yet a simple methodological problem remains: quantitative values are distributed across heterogeneous formats, with highly variable units of analysis, system boundaries, and documentary locators. This heterogeneity makes cumulative reuse difficult.

The aim of this paper is to document the data infrastructure associated with our quantitative review of LLMs and the environment. The primary contribution is an open dataset that stores extracted values together with comparability metadata and machine-access interfaces through an API and an MCP server. This infrastructure also serves as the basis for a first externalities estimator for practical use cases, such as estimating the environmental impact of a prompt, a software feature, or an annualized service relying on an LLM.

\section{Positioning}
The existing literature already provides robust anchor points, for example the training emissions of BLOOM \cite{luccioni2023}, the GPU-hours and emissions documented by Meta for Llama 3.1 \cite{meta2024llama}, production-scale inference measurements at Google \cite{elsworth2025}, water-footprint estimates \cite{li2025water}, and annual electricity trajectories for data centers \cite{iea2025,shehabi2024report,lbl2025,devriesgao2025joule}. However, the literature provides few open infrastructures designed to consolidate such results into a directly reusable form.

The \emph{LLM Environment Open Data} project addresses this gap through three simple design choices:
\begin{itemize}
\item one record per quantified value;
\item explicit traceability to the page, table, section, or paragraph of origin;
\item exposure of the data for both human readers and software agents.
\end{itemize}

\section{Dataset Construction Method}
The dataset is built from the quantitative literature review associated with the main publication on LLMs and the environment. For each retained source, we create one record for each quantified value that can be directly reused in the review. A single study may therefore generate several records when it reports multiple metrics or multiple units of analysis.

Each record contains the following fields:
\begin{itemize}
\item unique record identifier;
\item source key;
\item short citation;
\item publication year;
\item analytical phase: \texttt{training}, \texttt{inference}, \texttt{infrastructure}, \texttt{lifecycle};
\item impact category: carbon, energy, water, or compute;
\item metric name;
\item value and unit;
\item model or scope concerned;
\item geography;
\item temporal scope;
\item system boundary;
\item data type: measured, calculated, modeled, projected, estimated, or statistical;
\item source locator;
\item source URL;
\item free-text note.
\end{itemize}

This structure makes it possible to separate three dimensions that are often conflated in narrative reviews: the observed phenomenon, the way it was measured, and the exact level to which it applies.

\section{Initial Release Content}
The initial release contains 49 records spanning 11 sources. All four analytical phases are represented: 9 records for training, 17 for inference, 17 for infrastructure, and 6 for lifecycle effects. Energy and carbon are the most represented impact categories, followed by water.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{|L{2.8cm}|L{1.9cm}|L{2.2cm}|Y|}
\hline
Source & Phase & Example metric & Example value \\
\hline
Strubell et al. (2019) \cite{strubell2019} & training & training emissions & 626\,155 lb CO$_2$e \\
\hline
Luccioni et al. (2023) \cite{luccioni2023} & training & BLOOM 176B emissions & 24.7--50.5 tCO$_2$e \\
\hline
Meta (2024) \cite{meta2024llama} & training & Llama 3.1 405B GPU-hours & 30.84 million GPU-hours \\
\hline
Elsworth et al. (2025) \cite{elsworth2025} & inference & energy per prompt & 0.24 Wh/prompt \\
\hline
Ren et al. (2024) \cite{ren2024} & inference & energy per 500-word page & 0.0195 kWh/page \\
\hline
Li et al. (2025) \cite{li2025water} & lifecycle / water & GPT-3 total training water use & 5.4 million liters \\
\hline
IEA (2025) \cite{iea2025} & infrastructure & annual global data center electricity & 415 TWh in 2024 \\
\hline
Berkeley Lab (2025) \cite{lbl2025} & infrastructure & annual US data center electricity & 176 TWh in 2023 \\
\hline
de Vries-Gao (2025) \cite{devriesgao2025joule} & infrastructure & annualized global AI power demand & 46.4--201.5 TWh/year derived \\
\hline
Morrison et al. (2025) \cite{morrison2025} & lifecycle & total water over model creation pipeline & 2.769 million liters \\
\hline
\end{tabularx}
\caption{Representative records from the initial dataset release.}
\label{tab:dataset-overview}
\end{table}

\section{Formats and Schema}
The dataset is distributed in two synchronized forms: a canonical CSV file and a JSON export. The CSV facilitates human auditing, spreadsheet-based review, and version control. The JSON format facilitates programmatic integration into scripts, agents, or web interfaces. A JSON schema documents the expected structure and defines required fields.

The decision to maintain a canonical tabular source responds to a practical constraint: bibliographic extractions are often first reviewed and corrected manually. JSON is then regenerated from the CSV. This separation avoids multiplying sources of truth.

The main dataset is complemented by a second metadata table keyed by \texttt{record\_id}. This layer makes explicit estimation-oriented fields such as \texttt{unit\_of\_analysis}, \texttt{source\_type}, \texttt{uncertainty\_level}, \texttt{raw\_or\_derived}, and \texttt{applicability\_domain}. The methodological point is important: these fields are no longer only reconstructed in code, but published as data and therefore remain auditable, editable, and versionable.

\section{Local HTTP API}
The project provides a local HTTP API for rapid exploration of the dataset. The main endpoints are:
\begin{itemize}
\item \texttt{/health} to verify service status;
\item \texttt{/records} to list records with simple filters;
\item \texttt{/records/<record\_id>} to retrieve a specific record;
\item \texttt{/sources} to list distinct source studies;
\item \texttt{/stats} to retrieve summary statistics;
\item \texttt{/estimate} to produce prompt- or request-level estimates;
\item \texttt{/estimate\_feature} to produce annualized software-feature-level estimates.
\end{itemize}

This API is not intended to replace a full open-data platform. Rather, it provides a minimal programmatic reuse layer directly associated with the manuscript and the data repository \cite{software2026api}. The estimation endpoints extend the role of the dataset: it is no longer only an archive of reported values, but also a support for traceable calculations grounded in literature-derived factors.

\section{MCP Server}
The project also provides an MCP server over \texttt{stdio}. Its purpose differs from that of the API: it enables software agents to query the dataset directly as structured context, without scraping or heuristic reading of tables in the manuscript.

The current version exposes six tools:
\begin{itemize}
\item \texttt{list\_records};
\item \texttt{get\_record};
\item \texttt{aggregate\_by\_phase};
\item \texttt{list\_sources};
\item \texttt{estimate\_externalities};
\item \texttt{estimate\_feature\_externalities}.
\end{itemize}

This design addresses a precise need: enabling an assistant or software tool to recover a source-backed quantitative value, verify its analytical phase, and trace it back to the underlying publication without returning to an unrestricted reading of the PDF \cite{software2026mcp}.

\section{From Data to Estimation}
One of the central use cases of the project is the following question: ``Here is my prompt with a given LLM. Can you estimate the associated environmental externalities?'' To make that question computationally tractable, the dataset must be complemented by comparability metadata and by a factor-selection logic.

The current method follows four simple steps. First, the use case is classified into a relevant family, such as \emph{prompt-level inference} or \emph{query-level inference}. Second, the most appropriate factors are selected from the literature, prioritizing production measurements over report-level estimates. Third, these factors are adjusted to context when the user provides an electricity-mix carbon intensity or a water intensity. Fourth, the results are aggregated at the software-feature level and then over monthly or annual horizons.

The project therefore implements an explicit distinction between observed and derived values. For example, 0.24 Wh/prompt reported by Google \cite{elsworth2025} remains an observed reference value. By contrast, converting that value into a carbon estimate contextualized to a French electricity mix is a derived value and should be labeled accordingly.

\subsection{Final Calculation Equations}
Let $T_{in}$ and $T_{out}$ denote the input and output token counts for one LLM interaction, and let $T_{ref}=1550$ be the reference prompt size currently used by the implementation. The first adjustment is a token-scaling ratio:
\begin{equation}
r_{tok}=\mathrm{clamp}\left(\frac{T_{in}+T_{out}}{T_{ref}}, 0.25, 4.0\right).
\end{equation}

If $E_{prompt}$ is the literature-based energy factor in Wh per prompt and $n_{req}$ is the number of LLM requests considered in the scenario, the request-level energy estimate is:
\begin{equation}
E_{req}^{central}=E_{prompt}\times r_{tok}\times n_{req}.
\end{equation}
In the current implementation, the lower bound is set equal to this central value, while the upper bound uses a more conservative query-level factor $E_{query}^{high}$ from the literature:
\begin{align}
E_{req}^{low} &= E_{req}^{central},\\
E_{req}^{high} &= E_{query}^{high}\times r_{tok}\times n_{req}.
\end{align}

Carbon and water are then computed either from direct literature factors or from contextualized energy conversion. Let $CI$ be the electricity carbon intensity in gCO$_2$e/kWh and $WI$ the water intensity in L/kWh. The derived conversions are:
\begin{align}
C(E) &= \frac{E}{1000}\times CI,\\
W(E) &= \frac{E}{1000}\times WI \times 1000.
\end{align}
The factor 1000 in the last equation converts liters into milliliters. In practice, the estimator preserves the published prompt-level carbon and water factors as observed anchors, then expands the range with the contextualized values:
\begin{align}
C_{req}^{central} &= C\!\left(E_{req}^{central}\right),\\
C_{req}^{low} &= \min\!\left(C_{lit},\, C\!\left(E_{req}^{central}\right)\right),\\
C_{req}^{high} &= \max\!\left(C_{lit},\, C\!\left(E_{req}^{high}\right)\right),\\
W_{req}^{central} &= \max\!\left(W_{lit},\, W\!\left(E_{req}^{central}\right)\right),\\
W_{req}^{low} &= \min\!\left(W_{lit},\, W\!\left(E_{req}^{central}\right)\right),\\
W_{req}^{high} &= \max\!\left(W_{lit},\, W\!\left(E_{req}^{high}\right)\right),
\end{align}
where $C_{lit}$ and $W_{lit}$ are the direct prompt-level carbon and water factors extracted from the literature.

At the software-feature level, let $n_f$ be the number of LLM requests per feature use, $u_m$ the monthly number of feature uses, and $m_y$ the number of months per year. Annualized feature uses and annualized LLM requests are:
\begin{align}
U_{year} &= u_m \times m_y,\\
N_{LLM,year} &= U_{year}\times n_f.
\end{align}
The annual direct LLM impact is obtained by scaling the per-request range:
\begin{align}
E_{LLM,year} &= E_{req}\times N_{LLM,year},\\
C_{LLM,year} &= C_{req}\times N_{LLM,year},\\
W_{LLM,year} &= W_{req}\times N_{LLM,year}.
\end{align}

Finally, let $s_i$ denote the software overhead in Wh per feature use for technical component $i$. The annual overhead of each component is:
\begin{align}
E_{i,year}^{soft} &= s_i \times U_{year},\\
C_{i,year}^{soft} &= C\!\left(E_{i,year}^{soft}\right),\\
W_{i,year}^{soft} &= W\!\left(E_{i,year}^{soft}\right),
\end{align}
and the total annual software overhead is the sum over all components. The final annual result returned by the tool is therefore:
\begin{align}
E_{total,year} &= E_{LLM,year} + \sum_i E_{i,year}^{soft},\\
C_{total,year} &= C_{LLM,year} + \sum_i C_{i,year}^{soft},\\
W_{total,year} &= W_{LLM,year} + \sum_i W_{i,year}^{soft}.
\end{align}
These equations formalize the current implementation of the API and MCP estimator and clarify the distinction between source-backed factors and derived contextualized values.

\section{Research Questions}
The transition from evidence base to operational estimation leads to four research questions:
\begin{itemize}
\item Which metadata are required to make published environmental factors genuinely comparable and reusable?
\item To what extent can LLM externalities be estimated from the literature when direct provider telemetry is unavailable?
\item How should LLM calls be integrated into a broader software environmental accounting boundary?
\item How does machine-access exposure through an API and MCP improve practical reuse for environmentally responsible AI and software design?
\end{itemize}

\section{Implementation Example: From Prompt to Software Feature}
The current release includes a first-level estimator designed for screening use. For a standard text interaction, it first combines the most reusable inference factors in the corpus, notably 0.24 Wh/prompt, 0.03 gCO$_2$e/prompt, and 0.26 mL/prompt from production-scale measurements reported by Elsworth et al. \cite{elsworth2025}, together with the 2.9 Wh/query order of magnitude reported by EPRI \cite{epri2024} in order to construct a more conservative upper bound.

From these factors, the API and the MCP return a structured result including low, central, and high estimates, the selected factors, and the applied assumptions. For a demonstration scenario corresponding to a text interaction with 1\,200 input tokens and 350 output tokens under a simplified French electricity mix, the estimator returns 0.24 to 2.9 Wh per request, 0.0096 to 0.116 gCO$_2$e, and 0.096 to 1.16 mL of water.

The same engine can then be used at the software-feature level. For a conversational service used 10\,000 times per month over twelve months, with one LLM call per use and an additional software overhead of 0.15 Wh per use, the annualized estimate reaches 46\,800 to 366\,000 Wh, 1\,872 to 14\,640 gCO$_2$e, and 18\,720 to 146\,400 mL of water. The important point is not only the final figure, but the decomposition between direct LLM impact and software-system overhead.

This implementation is deliberately conservative. It does not claim to provide an auditable environmental assessment in the sense of formal carbon accounting. Rather, it provides a traceable, reproducible, and extensible estimate that is useful for eco-design, use-case prioritization, and the later development of more robust methods for software environmental accounting.

\section{Evaluation Across Three Software Scenarios}
To assess the usefulness of the proposed framework, we applied the estimator to three representative software scenarios: a support chatbot, a retrieval-augmented assistant, and a batch-generation workflow. All scenarios rely on the same estimation logic, but differ in token volume, number of LLM calls per use, monthly usage volume, and software overhead.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{|L{2.6cm}|L{2.1cm}|L{2.1cm}|L{2.1cm}|Y|}
\hline
Scenario & Annual volume & Total annual energy & Total annual carbon & Total annual water \\
\hline
Support chatbot & 120\,000 uses/year & 46\,800--366\,000 Wh & 1\,872--14\,640 gCO$_2$e & 18\,720--146\,400 mL \\
\hline
RAG assistant & 48\,000 uses/year & 61\,737.6--506\,553.6 Wh & 2\,467.2--20\,265.6 gCO$_2$e & 24\,691.2--202\,617.6 mL \\
\hline
Batch generation & 600\,000 uses/year & 410\,340--4\,426\,080 Wh & 16\,440--177\,060 gCO$_2$e & 164\,160--1\,770\,420 mL \\
\hline
\end{tabularx}
\caption{Demonstration results across three software scenarios.}
\label{tab:three-scenarios}
\end{table}

These results illustrate three properties of the method. First, moving from unit-level usage to annualized software features substantially changes the relevant order of magnitude for decision-making. Second, non-LLM software overhead is not negligible and should be tracked explicitly. Third, the spread between low and high bounds remains large, confirming that the tool should be used as a screening and relative-comparison instrument rather than as a definitive measurement device.

\section{Scientific Contribution}
The main contribution is not algorithmic but infrastructural. The publication associated with the dataset transforms a literature review into a reusable evidence base. This changes the nature of the scientific deliverable in four ways.

First, it separates the narrative layer of the paper from the reusable factual layer. Second, it enables incremental updates of the extraction base without rewriting the entire synthesis. Third, it provides a common support for comparing results that belong to different analytical levels, such as a query, a prompt, a generated page, a final training run, or an annual data center electricity figure. Fourth, it serves as an operational basis for an externalities estimator for software systems integrating LLMs, thereby bringing the quantitative review closer to the concerns of responsible digital design and software environmental accounting.

\section{Limitations}
The dataset does not eliminate the underlying heterogeneity of the literature; it makes that heterogeneity explicit. Values remain dependent on assumptions, calculation methods, and system boundaries. Some sources are peer-reviewed papers, while others are model cards or technical reports. The dataset therefore includes metadata on evidence type and scope precisely to reduce invalid comparisons.

The current release is also partial. It primarily covers the values mobilized in the associated review paper and the major sources identified so far. The objective is not to claim immediate exhaustiveness, but to provide an open infrastructure that can be enriched, audited, and corrected over time.

\section{Conclusion}
This paper accompanies the release of \emph{LLM Environment Open Data}, a structured dataset on quantified environmental impacts of LLMs and their infrastructure. By combining an open dataset, a layer of comparability metadata, an HTTP API, and an MCP server, it proposes an augmented form of scientific publication: readers receive not only a synthesis, but also the evidence layer that makes that synthesis verifiable, extensible, and automatable \cite{dataset2026,software2026api,software2026mcp}.

The next logical step is to expand the source corpus and publish the dataset with a persistent identifier. At this stage, the project already provides the necessary scaffolding to treat the dataset as a scientific object in its own right, while also serving as the starting point for a method of environmental accounting for software systems integrating LLMs.

\bibliographystyle{plain}
\bibliography{references_llm_environment_opendata}

\end{document}
